// Important: use lowercase headers to avoid unexpected errors
const HTTP_HEADERS = {
  CONTENT_TYPE: 'content-type',
  AUTHORIZATION: 'authorization',
  ACCEPT: 'accept',
} as const;

export { HTTP_HEADERS };
